<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.account.mapper.UserDao">
  <resultMap id="BaseResultMap" type="com.account.bean.User">
    <result column="id" jdbcType="INTEGER" property="id" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="username" jdbcType="VARCHAR" property="username" />
  </resultMap>
  <insert id="saveuser" parameterType="com.account.bean.User">
    insert into user (id, phone, username
      )
    values (#{id,jdbcType=INTEGER}, #{phone,jdbcType=VARCHAR}, #{username,jdbcType=VARCHAR}
      )
  </insert>
  <select id="finduser" parameterType="com.account.bean.User" resultType="com.account.bean.User">
    select * from uesr where phone=#{phone}
  </select>

</mapper>